---
import Container from "@components/Container.astro";
import { Picture } from "astro:assets";
import venueImage from "@images/home/venue.webp";
---

<section>
	<Container>
		<h2 id="venue">Venue</h2>

		<div>
			<div>
				<Picture
					src={venueImage}
					alt="CDN Austria Venue - Cineplexx Wienerberg"
					formats={["avif", "webp"]}
				/>
				<a
					href="https://maps.app.goo.gl/Q22METdvy3f1GFfx8"
					target="_blank"
					rel="noopener noreferrer"
				>
					<strong>Cineplexx Wienerberg</strong>
					<span>Wienerbergstraße 11, 1100 Wien</span>
				</a>
			</div>

			<div>
				<p>
					Cloud Native Days Austria are held at{" "}
					<a
						href="https://maps.app.goo.gl/Q22METdvy3f1GFfx8"
						target="_blank"
						rel="noopener noreferrer"
					>
						Cineplexx Wienerberg
					</a>
					. You'll enjoy two cinema-style stages with comfy seats, great sound, and clear visuals.
				</p>
				<p>
					Grab a coffee or a bite to eat throughout the day and check out our sponsors for cool
					swag. There's plenty of space to meet new people and catch up with friends.
				</p>
				<p>Stick around for our evening event – it's right nearby!</p>
				<p>
					<strong>We can't wait to see you there!</strong>
				</p>
			</div>
		</div>
	</Container>
</section>

<style>
	section {
		padding: var(--space-12) 0;
	}

	section :global(.container) {
		max-width: var(--container);
	}

	h2 {
		text-align: center;
		margin-bottom: var(--space-8);
	}

	section > :global(.container) > div {
		display: flex;
		gap: var(--space-8);
		align-items: flex-start;
	}

	@media (max-width: 768px) {
		section > :global(.container) > div {
			flex-direction: column;
		}
	}

	section > :global(.container) > div > div:first-child {
		flex: 1;
		text-align: center;
		display: flex;
		flex-direction: column;
		align-items: center;
		gap: var(--space-4);
	}

	section > :global(.container) > div > div:first-child img {
		max-height: 300px;
		width: auto;
		border-radius: var(--radius-md);
		object-fit: contain;
	}

	section > :global(.container) > div > div:first-child a {
		display: block;
		color: var(--color-text);
		text-decoration: none;
		transition: color var(--transition-fast);
	}

	section > :global(.container) > div > div:first-child a:hover {
		color: var(--color-pink);
	}

	section > :global(.container) > div > div:first-child strong {
		display: block;
		font-family: var(--font-heading);
	}

	section > :global(.container) > div > div:first-child span {
		font-size: var(--text-sm);
	}

	section > :global(.container) > div > div:last-child {
		flex: 1;
	}

	section > :global(.container) > div > div:last-child p {
		margin-bottom: var(--space-4);
	}

	section > :global(.container) > div > div:last-child p:last-child {
		margin-bottom: 0;
	}

	section > :global(.container) > div > div:last-child a {
		color: var(--color-purple);
	}

	section > :global(.container) > div > div:last-child a:hover {
		color: var(--color-pink);
	}

	@media (max-width: 768px) {
		section > :global(.container) > div > div:last-child {
			text-align: left;
		}
	}
</style>
